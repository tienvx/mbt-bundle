language: php

php:
  - 7.1
  - 7.2
  - nightly

matrix:
  allow_failures:
    - php: nightly

install:
  - composer install

script:
  - composer run-script cs-fix
  - composer run-script coverage

after_success:
  - composer run-script coverall
