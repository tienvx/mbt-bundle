The <info>%command.name%</info> command generates a new generator class.

<info>php %command.full_name% generator-name CoolStuffGenerator</info>

If the argument is missing, the command will ask for the generator name and generator class name interactively.
