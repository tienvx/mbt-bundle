The <info>%command.name%</info> command generates a new model (workflow) configuration file.

<info>php %command.full_name% model-name</info>

If the argument is missing, the command will ask for the model name interactively.
