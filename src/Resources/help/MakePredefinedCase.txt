The <info>%command.name%</info> command generates a new pre-defined case (test case) configuration file.

<info>php %command.full_name% pre-defined-case-name</info>

If the argument is missing, the command will ask for the pre-defined case name interactively.
