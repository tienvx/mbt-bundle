The <info>%command.name%</info> command generates a new workflow configuration file.

<info>php %command.full_name% workflow-name</info>

If the argument is missing, the command will ask for the workflow name interactively.
